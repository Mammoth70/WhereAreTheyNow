<?xml version="1.0" encoding="utf-8"?>
<!-- Layout для MainActivity (UsersList). -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/frameMainActivity"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".MainActivity">

    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <!-- Top AppBar. -->
        <include layout="@layout/partial_app_bar" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/itemUsersRecycler"
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            app:layout_behavior="@string/appbar_scrolling_view_behavior"
            android:paddingBottom="100dp"
            android:clipToPadding="false"
            tools:listitem="@layout/item_user"
            tools:itemCount="6" />

        <!-- Кнопка FAB. -->
        <include layout="@layout/partial_fab_main" />
    </androidx.coordinatorlayout.widget.CoordinatorLayout>

    <!-- Bottom navigation bar. -->
    <include layout="@layout/partial_bottom_navigation" />
</LinearLayout>